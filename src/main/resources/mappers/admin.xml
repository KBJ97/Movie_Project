<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinema.admin.mapper.MovieMapper">

    <insert id="insertMovie">
        INSERT INTO movie (movieName, director, actor, cateNum,  runTime, openDate, movieAge,  story)
        VALUES (#{movieName}, #{director}, #{actor}, #{cateNum}, #{runTime}, #{openDate}, #{movieAge}, #{story})
    </insert>


</mapper>